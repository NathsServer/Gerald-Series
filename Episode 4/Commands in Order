Prerequisites

Docker & Docker Compose installed
n8n running (see Episode 1)
Brave Search API key — https://api.search.brave.com/app/keys (free tier, set a $5 monthly limit)
GitHub Personal Access Token — https://github.com/settings/tokens (classic, repo scope)

Add to .env:
BRAVE_API_KEY=your_brave_api_key
GITHUB_TOKEN=your_github_pat

Update docker-compose.yml with the one in the Episode 4 folder

sudo docker compose up -d

curl http://localhost:8881/sse  # Filesystem
curl http://localhost:8882/sse  # Brave Search
curl http://localhost:8883/sse  # GitHub
curl http://localhost:8884/sse  # Ollama
curl http://localhost:8885/sse  # Postgres
```

Each should return:
```
event: endpoint
data: /message?sessionId=...

MCP reference links

Filesystem — https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem
Brave Search — https://github.com/modelcontextprotocol/servers/tree/main/src/brave-search
GitHub — https://github.com/github/github-mcp-server
Ollama — https://github.com/ergut/mcp-ollama-server
Postgres — https://github.com/modelcontextprotocol/servers/tree/main/src/postgres
Supergateway — https://github.com/supercorp-ai/supergateway


